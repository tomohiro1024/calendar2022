class Holiday {
  String title;
  String explanation;
  DateTime startAt;
  DateTime endAt;

  Holiday(
      {required this.title,
      required this.explanation,
      required this.startAt,
      required this.endAt});
}
